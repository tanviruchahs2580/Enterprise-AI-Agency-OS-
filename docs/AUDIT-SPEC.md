# AUDIT-SPEC v1.0

Every important action → immutable audit event: actor/agent/task/workflow/tool/input/output hash/policy/artifact/commit/test/approval/deployment (§56). Multi-tenancy org/project/env/workspace/agent/policy/secret/artifact/audit isolation (§57), Secrets via manager never raw LLM (§58), Human governance risk-based (§52) + takeover pause→terminate (§53).

Query: `GET /api/v1/audit/events` + `audit_events` table.
